{
  "name": "dbx-split",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "./in-each-module install",
    "build": "node buildAll.mjs",
    "build:watch": "./in-each-module build:watch",
    "reset": "./in-each-module reset && rm -rf */dist node_modules",
    "fix": "./in-each-module fix",
    "format": "./in-each-module format",
    "test": "./in-each-module test",
    "test:lint": "./in-each-module test:lint",
    "test:prettier": "./in-each-module test:prettier",
    "test:unit": "./in-each-module test:unit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
